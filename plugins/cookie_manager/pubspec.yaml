name: cookie_manager
description: A cookie manager for Web, which supports persistent cookies in RAM and file.
version: 2.0.0
homepage:  https://gitlab.com/mobiplay/web/-/tree/master/plugins/cookie_manager

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
#  web_cookies: ^1.0.1
  web_cookies:
    git:
      url: https://gitlab.com/mobiplay/web_cookies
#  web: ^3.0.0
  web:
    path: ../../web

dev_dependencies:
  test: ^1.16.2
  test_coverage: ^0.5.0
  pedantic: '1.10.0'

